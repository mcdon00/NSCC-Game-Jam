function Player(canvas){

	var stage = createjs.Stage(canvas);
	this = createjs.Shape();
	this.player.graphics.beginStroke("white").drawPolyStar(0, 0, 15, 3, 0, angle);
	player.y = window.innerHeight/2;
	player.x = (window.innerWidth/2) + 200;

}